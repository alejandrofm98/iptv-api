[project]
name = "iptv-api"
version = "2.1.0"
description = "IPTV API: users, devices, content catalog, HLS stream proxy"
requires-python = ">=3.12"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
include = ["iptv_api*"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]
extend-exclude = [
    "data/m3u",
    ".venv",
    "video-extractor-service",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
ignore = [
    "E501",   # line-too-long (lo maneja el formatter)
    "B008",   # FastAPI Depends() en signatures es idiomático
    "B023",   # function definition does not bind loop variable — real pero requiere refactor async
    "UP015",  # redundant open() mode — molesta con archivos m3u grandes
    "UP042",  # class inherits from str and enum.Enum — estilo, no rompe nada
    "UP046",  # Generic class uses Generic subclass — Python 3.12 type params
    "RUF001", # ambiguous EN DASH — intencional en regex de series_repo
    "RUF012", # mutable class attribute defaults — diseño intencional del codebase
    "SIM105", # contextlib.suppress — estilo, no crítico
]
unfixable = ["B"]

[tool.ruff.lint.per-file-ignores]
"src/iptv_api/main.py" = ["B008", "E402", "F401", "F811"]
"tests/*" = ["B011", "B017", "E402"]
"video-extractor-service/*" = ["ALL"]

[tool.ruff.lint.isort]
known-first-party = ["iptv_api"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
warn_unused_ignores = true
warn_return_any = false
disallow_untyped_defs = false
no_implicit_optional = true
show_error_codes = true
exclude = [
    "video-extractor-service/",
    ".venv/",
    "data/m3u/",
]

[[tool.mypy.overrides]]
module = "iptv_api.main"
ignore_errors = true
# F6a: entry point moved from scripts/api.py

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[[tool.mypy.overrides]]
module = ["iptv_api.services.*"]
ignore_errors = true
# Services migrated to iptv_api/services/ in F5c

[tool.vulture]
min_confidence = 80
paths = ["src/iptv_api"]
sort_by_size = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--tb=short",
]
markers = [
    "integration: marks tests as integration (deselect with '-m \"not integration\"')",
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/__pycache__/*",
    "*/tests/*",
    "*/.venv/*",
    "video-extractor-service/*",
    "src/iptv_api/main.py",  # F6a: entry point
]

[tool.coverage.report]
precision = 1
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "def __repr__",
]
